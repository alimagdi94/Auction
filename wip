# Role
You are an Elite Algorithmic Trading Developer specializing in MQL5 and CCanvas UI rendering.

# Task
Please apply a critical visual fix to my Footprint indicator. [cite_start]The current font scaling logic is resulting in text that doesn't scale properly when zoomed in, because it calculates the font size based solely on the cell's height (`cellH`) [cite: 504-505, 558-559]. I need to implement "Bi-Directional Dynamic Scaling" so the text perfectly maximizes its size without bleeding horizontally.

# Specific Refactoring Requirements

## 1. Bi-Directional Cell Text Scaling
* [cite_start]**Issue:** In the `DrawBar` function, `cellFontSz` is calculated as `(int)(cellH * 0.55)`[cite: 504]. When the chart is zoomed in vertically, `cellH` becomes very large, but the column width (`halfW`) might still be narrow. Scaling based only on height causes clipping or improper rendering.
* [cite_start]**Requirement:** Replace the `cellFontSz` logic [cite: 504-505] with a bidirectional calculation. 
    1. Calculate max height allowed: `int maxH = (int)(cellH * 0.55);`
    2. Calculate max width allowed: `int maxW = (int)(halfW * 0.45);` (Since text is Bid or Ask, it must fit inside half the bar width).
    3. Take the minimum of the two: `int cellFontSz = MathMin(maxH, maxW);`
    4. Clamp the final result safely: `cellFontSz = MathMax(8, MathMin(cellFontSz, 28));`

## 2. Bi-Directional Bottom Label Scaling
* [cite_start]**Issue:** The Total Volume and Delta labels at the bottom of the bar suffer from the same issue [cite: 558-559]. 
* **Requirement:** Apply the same logic to `lblFontSz`. 
    1. Because this text spans the *entire* candle width (not just half), use `barW`: `int maxW = (int)(barW * 0.25);`
    2. `int maxH = (int)(cellH * 0.50);`
    3. `int lblFontSz = MathMin(maxH, maxW);`
    4. Clamp it safely: `lblFontSz = MathMax(8, MathMin(lblFontSz, 24));`

# Context: Current Code
only touch footprint.full