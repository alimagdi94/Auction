Please rewrite the Render loop, DrawGridCell, and Object Pool in my MQL5 footprint code to achieve exact structural parity with my target chart. Please implement the following functional fixes:

1. Overlay Directly on the Candles (Center Alignment):
Currently, the code detaches the footprint columns and draws them sequentially in empty space to the right of the chart.

Action: Remove the sequential current_x offset logic completely.

Calculate x_left so that the footprint column is perfectly centered directly over its corresponding native candle using GetBarX(bt). The footprint must overlay the candle it belongs to.

2. The Histogram Background Track:
The histogram bars currently float over the chart background.

Action: Add a solid background rectangle behind the entire footprint column (spanning barWidthPx + MAX_HIST_W). This gives the histogram area a uniform, defined track to sit on, regardless of how short the individual volume bars are.

3. 1px Rectangles for Dotted Grid Lines:
Do not use text strings like "........" or "⋮" for grid lines.

Action: Use OBJ_RECTANGLE_LABEL with a height/width of 1 pixel to draw the grid.

Draw a 1px vertical line exactly down the center of the Bid/Ask text cells.

Draw a 1px horizontal line at the bottom of each row that spans the entire width (text cells + histogram track) to act as the row separator.

4. Text Font and Precise Anchoring:

Change the font from "Consolas" to a cleaner sans-serif font like "Arial" or "Tahoma".

Anchor the Bid text (ANCHOR_RIGHT_UPPER) exactly 2 pixels to the left of the center vertical line.

Anchor the Ask text (ANCHOR_LEFT_UPPER) exactly 2 pixels to the right of the center vertical line.

5. POC vs. Imbalance Bounding Boxes:

POC Box: The Point of Control highlight border must span the entire width of the structure (barWidthPx + MAX_HIST_W), wrapping both the text cells and the histogram bar.

Stacked Imbalance Box: The stacked imbalance highlight border must strictly enclose only the text cells (width = barWidthPx), stopping exactly where the histogram track begins.

Please provide the fully updated code snippets focusing strictly on these coordinate, anchoring, and object placement mathematical fixes.