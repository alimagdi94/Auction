You are an expert MQL5 developer (MT5) specializing in footprint/cluster chart rendering using OBJ_RECTANGLE_LABEL and OBJ_LABEL.

PROBLEM
My footprint ladder columns are rendered far away from the candles. They do not track the candle positions when I scroll/zoom. I need the footprint to be anchored to each candle on the chart.

GOAL
Refactor my code so each footprint column is positioned relative to the candle’s true screen X coordinate:
- For each bar i, compute the candle X using ChartTimePriceToXY(chart_id, subwindow, time[i], price_ref, x, y).
- Use that X as the anchor for the footprint column (e.g., footprint_left = candle_x + x_offset_px).
- The footprint must remain attached to candles under scrolling, zooming, and timeframe changes.

REQUIREMENTS (NON-NEGOTIABLE)
1) Replace any manual x math like:
   x = panel_left + bar_index * bar_width_px
   OR any use of fixed panel coordinates that ignores chart scaling.
   with ChartTimePriceToXY-based anchoring per bar.

2) Correct Y mapping for the ladder:
   - Determine row step in price: step = tick_size * ticks_per_row (or price_step_points*Point).
   - For each price level in a bar, get y using ChartTimePriceToXY(time[i], level_price, x, y).
   - Use that y for cell placement so rows align to real prices and do not stretch incorrectly.

3) Column placement rules:
   - The footprint column should appear adjacent to the candle (configurable: LEFT of candle, RIGHT of candle, or CENTERED).
   - Inputs:
     - column_mode = {right_of_candle, left_of_candle, centered}
     - x_offset_px (default small, e.g. 2–6 px)
     - bar_width_px (controls the footprint width)
     - bar_gap_px
   - Ensure columns do not overlap neighboring candles excessively.

4) Visible bars only:
   - Determine visible bar range and render only those bars.
   - Use ChartGetInteger(chart_id, CHART_FIRST_VISIBLE_BAR) and CHART_VISIBLE_BARS to iterate visible bars.
   - Or otherwise reliably compute visible indices.

5) Object update strategy:
   - Reuse objects (pooling). Do not recreate every tick.
   - Update only changed bars on new ticks, and full refresh on zoom/scroll events if needed.

DELIVERABLES
- Provide full updated .mq5 code.
- Identify the exact reason my current footprint is “far away” (which variables/logic cause the offset).
- Show the new functions you add:
  - GetBarX(i) using ChartTimePriceToXY
  - GetPriceY(time[i], price) using ChartTimePriceToXY
  - RenderFootprintBar(i, x_anchor)

Here is my code:
footprint
the file that has no extention footprint is our target file