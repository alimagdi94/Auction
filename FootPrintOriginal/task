Please update my FootPrintOriginal\footprint code to match the exact visual style and layout of the provided target screenshot. Based on my current output and the target image, please implement the following fixes:

1. Fix the Column Squishing Bug (Strict Width Enforcement):
In my current code, the columns are shrinking and overlapping because of this line in Render():
if(maxW > 0) barWidthPx = (maxW >= InpMinBarWidthPx) ? MathMin(barWidthPx, maxW) : maxW;
When zoomed out, this logic completely bypasses the minimum width and squishes the columns to tiny pixel widths, making the text illegible. Please rewrite the rendering logic to enforce a strict, fixed InpBarWidthPx (e.g., 85px). To prevent overlap when the chart is zoomed out, detach the grid from GetBarX() and draw the columns sequentially from right to left with a fixed gap (InpBarGapPx), ignoring the native candle X-coordinates.

2. Light Theme & Text Color:
My current code uses a dark theme (black backgrounds, white text). Update the color inputs and drawing logic to match the target's light-cell theme:

Text Color: Change all Bid and Ask text to Black for readability.

Cell Backgrounds: Use a light beige/cream color for empty/low-volume cells. Use light pastel red/pink for bearish delta rows and light pastel green for bullish delta rows (or Value Area highlights).

3. Cell Layout & Dotted Separators:

Vertical Separator: Add a dotted vertical line down the exact center of each footprint column to separate the Bid and Ask values.

Horizontal Grid: Change the row separator lines to a dotted style rather than solid blocks.

Text Alignment: Ensure Bid numbers are aligned to the right edge of the left half, and Ask numbers are aligned to the left edge of the right half, sitting neatly beside the center vertical dotted line.

4. Imbalance & POC Outlines (Not Solid Fills):
Currently, my imbalances fill the entire cell with a dark, solid color (InpImbSellColor), making text hard to read. In the target image, imbalances and the POC are highlighted using thick borders/outlines with transparent inner backgrounds:

Imbalances: Use a thick Red border for sell imbalances and a thick Green border for buy imbalances.

Stacked Imbalances: Draw a thick continuous border around the consecutive imbalance cells.

POC (Point of Control): Highlight the POC row with a thick Black or Orange border, rather than a solid background fill.

Please provide the updated Footprint.mq5 code with these specific layout and cosmetic changes applied.