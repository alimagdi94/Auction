I have an MQL5 Chart Trader panel. The trading logic and risk math are accurate, but the code suffers from severe UI freezing during panel dragging and high CPU usage during `OnTick` visual line updates.

Please refactor the provided MQL5 code based on the following strict requirements. 

**CRITICAL CONSTRAINT: STRICTLY NO OOP.** Do not convert the UI or trading logic to classes. Do not use object pointers. Maintain the exact procedural architecture currently in use.

Apply ONLY the following critical performance fixes:

**1. Fix the Dragging Bottleneck in `HandleMouseMove` (Severe):**
* Currently, the drag logic loops over `ObjectsTotal(0, 0, -1)` and uses `StringFind` on every chart object for every pixel moved. 
* Fix this by introducing a global array `string g_ui_elements[]`. 
* Inside `CreateBtn`, `CreateEdit`, `CreateLbl`, and when creating backgrounds/separators, add the object's name to this array.
* Update `HandleMouseMove` to loop ONLY through `g_ui_elements[]` and apply the `XDISTANCE` and `YDISTANCE` offsets, bypassing the need to search the entire chart. Ensure `LINE_SL`, `LINE_TP`, `LINE_ENTRY`, `LINE_ENTRY_ZONE`, and `LBL_ZONE_*` are excluded from this array.

**2. Optimize `DrawVisualLines` (Reduce `ObjectFind` calls):**
* `DrawVisualLines` runs on every tick and calls `ObjectFind` multiple times. 
* Introduce global boolean flags (e.g., `bool g_line_sl_exists = false;`) to track the state of your visual lines and labels.
* Update these flags to `true` when `ObjectCreate` is called, and `false` when `TryDeleteObj` is called. Use these flags instead of `ObjectFind()` inside the high-frequency tick loop.

**3. Cleanup `CreateGUI` Array Reset:**
* Ensure `g_ui_elements[]` is emptied (`ArrayResize(g_ui_elements, 0)`) at the beginning of `CreateGUI()` before it gets repopulated.

**4. Preserve Everything Else:**
* Keep all trading math, risk calculations, hotkeys, and the click-and-hold repeat timer exactly as they are.

Please provide the updated code focusing specifically on implementing these procedural performance fixes.