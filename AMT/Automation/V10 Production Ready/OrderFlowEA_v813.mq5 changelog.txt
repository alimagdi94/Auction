Both targets confirmed. Applying the two passes.
Pass 1 — MANAGE-SLEEP: Replace Sleep(200) in ManagePositions() with a 50 ms spin-wait.Pass 1: Replace Sleep(200) in ManagePositions SL-modify retry with GetTickCount64 spin-waitPass 1: Replace Sleep(200) in ManagePositions SL-modify retry with GetTickCount64 spin-waitPass 2 — TRAIL-VALIDATE: Add four bounds checks inside if(InpATEnable) after the TP validation block.Pass 2: Add InpBreakEven and InpTrail parameter validation inside the if(InpATEnable) blockPass 2: Add InpBreakEven and InpTrail parameter validation inside the if(InpATEnable) blockVersion bump + changelog header:Edited a file, ran a commandEdited a file, ran a commandFull verification checklist:Full verification checklistFull verification checklistAll checks pass. No Sleep(200) active anywhere — only in changelog comments. Both spin-waits live in the correct functions. All four validation checks present with INIT_PARAMETERS_INCORRECT, gated by the correct feature flags.Verified previously applied fixes and confirmed file completionVerified previously applied fixes and confirmed file completionThese two fixes were already applied in the previous pass — the v8.13 patch was completed before this prompt arrived. Let me do a quick re-verification against the uploaded spec to confirm everything matches exactly.Verify v813 output against uploaded patch specVerify v813 output against uploaded patch specEverything matches the spec exactly. The file was already complete from the previous pass.Presented filePresented fileAll checklist items confirmed against the uploaded spec:

✅ Sleep(200) zero active calls anywhere in the file — both instances replaced with GetTickCount64() spin-waits capped at 50 ms (one in trade_Send() from v8.12, one in ManagePositions() from this patch)
✅ InpBreakEvenTrigger <= 0.0 → INIT_PARAMETERS_INCORRECT, guarded by if(InpUseBreakEven)
✅ InpBreakEvenBuffer < 0.0 → INIT_PARAMETERS_INCORRECT, guarded by if(InpUseBreakEven)
✅ InpTrailStep <= 0.0 → INIT_PARAMETERS_INCORRECT, guarded by if(InpUseTrailing)
✅ InpTrailStart < 0.0 → INIT_PARAMETERS_INCORRECT, guarded by if(InpUseTrailing)
✅ version and EA_VERSION both "8.13" — 3,571 lines