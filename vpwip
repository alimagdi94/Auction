# Role
You are an Elite Algorithmic Trading Developer specializing in MQL5, system architecture, and ultra-low latency UI rendering.

# Task
Please apply the final polish and edge-case optimizations to my `CCanvas`-based Volume Profile indicator. The code is structurally sound and highly optimized, but it requires two specific fixes to resolve a Strategy Tester optimization bug and an X-axis rendering artifact related to weekend time gaps.

# Specific Refactoring Requirements

## 1. Strategy Tester Optimization Carryover Bug
* [cite_start]**Issue:** The `static datetime tester_last_time` declared inside `OnCalculate` [cite: 87, 88] will retain its value across different optimization passes in the Strategy Tester, permanently breaking the throttle logic after the first pass.
* [cite_start]**Requirement:** Move `tester_last_time` to the global scope alongside the other state variables like `g_last_render_ms` [cite: 36-40]. [cite_start]Explicitly reset it to `0` inside the `OnInit()` function [cite: 77-80] to ensure a clean slate for every optimization pass.

## 2. X-Axis "Time Gap" Render Fix (Pure Pixel Space)
* [cite_start]**Issue:** Calculating histogram widths using time variables like `time_width_total` [cite: 223] causes severe rendering bugs when the profile spans across weekends or missing data gaps. [cite_start]Furthermore, calling `ChartTimePriceToXY` inside the loop [cite: 234] maps these gaps to a single pixel, distorting the volume bars.
* **Requirement:** Refactor the X-axis math within the `DrawBars` function. [cite_start]Since `x_start` and `x_end` are already queried exactly once outside the loop [cite: 216-218], abandon time-based width calculations entirely. [cite_start]Calculate the bar widths purely in pixel space using `max_width_factor`[cite: 213]. [cite_start]Completely eliminate the remaining `ChartTimePriceToXY` queries from the inner loop [cite: 234] [cite_start]and assign physical pixel coordinates (`x1`, `xsplit`, `x2`) based on the `InpAlignment` parameter [cite: 226-229].